/*const addtwonumbers = (firstnumber, secondnumber) => {
    return firstnumber + secondnumber
}
// calling your function
console.log(addtwonumbers(45,89));

const evenNumbers = () => {
    for ( let i=1; i <= 100; i++){
        if(i % 2 == 0) {
        console.log(`${i} is even`)
        }
    }
}
evenNumbers();

const fullName = (firstname,middlename, lastname) => {
    return`${firstname} ${middlename} ${lastname}`

}  
console.log(fullName('vivian', 'naana', 'amakye'))
*/
/*
let PersonObject = {
    firstName: 'Asante',
    middleName: 'Adarkwa',
    lastName: 'Usman',
    age: 35
}

const fullName = (firstName,middleName,lastName) => {
    return `${firstName} ${middleName} ${lastName} `
}

console.log(fullName('Asante', 'Adarkwa','Usman'))

*/
let PersonObject = {
    firstName: 'Asante',
    middleName: 'Adarkwa',
    lastName: 'Usman',
    age: 35
}
 
const adding = () => {
    PersonObject.gender == 'male'
    PersonObject.weight == 75
    console.log(adding('gender', 'weight'))
}
console.log(adding)
